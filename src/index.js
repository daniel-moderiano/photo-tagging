import './styles/style.scss';
import './scripts/controller';
import './scripts/timer';
import './scripts/leaderboard';

// Importing index.html template here ensures that the page is reloaded in the browser when the template HTML changes. This should be removed for the production build
import './index.html';
